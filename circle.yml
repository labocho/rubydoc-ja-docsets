database:
  override:
    - echo "no database"
test:
  override:
    - echo "no test"
deployment:
  production:
    branch: circleci
    commands:
      - bundle exec ruby scripts/build_and_pull_request.rb
