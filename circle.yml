machine:
  ruby:
    version:
      2.6.0
database:
  override:
    - echo "no database"
test:
  override:
    - echo "no test"
deployment:
  production:
    branch: master
    commands:
      - >
        if [ -n "${BUILD_DOCSETS}" ]; then
          bundle exec ruby scripts/build_and_pull_request.rb;
        fi
